# QuantaEX 전수조사 리포트 #3 (가입→거래→스테이킹→바이너리 매칭)

- 작성일: 2026-08-27
- 범위: 가입 · 로그인/이메일인증 · KYC · 입금/잔액 · 거래(주문/체결/수수료) · 출금 · 스테이킹 · 바이너리 수동배치/매칭(cron)
- 방법: (A) 코드 레벨 정독 + (B) **프로덕션과 동일한 Hono worker(`wrangler pages dev`)를 로컬 D1로 띄워 실제 HTTP·DB 레벨 E2E 실행**
- 결과 요약: **치명 결함 없음.** 핵심 정책·자금 안전·순서 독립성 모두 통과. 정책 확인/개선이 필요한 항목 3건(P2/P3) 별도 명시.

---

## 0. 조사 방식에 대한 중요 메모
`npm run dev`는 **레거시 Express 서버(`server/index.js`, 포트 3001)**를 띄우며, 이는 실제 배포되는 코드(`src/server/index.ts` = Hono, Cloudflare Pages Functions)와 **다릅니다.** 따라서 이번 조사는 프로덕션과 100% 동일한 코드를 검증하기 위해 `npm run dev:pages`(= `wrangler pages dev dist --d1=quantaex-production --local`)로 실제 worker를 로컬 D1과 함께 구동하여 테스트했습니다.

---

## Stage 1. 가입 + 추천 (PASS)
| 항목 | 결과 |
|---|---|
| 정상 가입 | 200, JWT + referral_code 발급 |
| 추천코드로 가입 시 L1 보상 | **L1 직접 추천인에게 100 QX 즉시 지급** (`referrer_credited_qta:100`) |
| 잘못된 추천코드 | **400 "Invalid referral code"** (사용자 미생성) |
| 약한 비밀번호(8자 미만/문자·숫자 미포함) | **400** 거부 |
| 신규 계정 초기 QX | **0** (웰컴 보너스는 가입 시 미지급 — 어뷰징 차단) |
| 바이너리 배치 | `binary_parent_id`만 설정, `binary_leg = NULL`(미배치) — 사장님이 나중에 선택 |

- **경미(P3):** `auth.ts` L451-454 주석이 옛 "자동 배치(smaller leg)"를 설명하나 실제 코드는 수동배치. **주석만 낡음**(동작 영향 없음).

## Stage 2. 로그인 / 이메일 인증 (PASS)
| 항목 | 결과 |
|---|---|
| 웰컴 보너스 지급 시점 | **첫 이메일 인증 시** 100 QX 지급 (`firstVerification` 가드로 재인증해도 중복 지급 없음) |
| 회사발행 표시 | `available_initial` 동반 증가 → 거래는 가능하나 외부 출금 불가 |
| 인증 링크 재전송(24h 만료) | **이번 세션에서 수정 완료** — 신규 `/auth/resend-verification`(로그인 전용, Turnstile 불필요) + 배너 catch/에러표시 (commit 29d144c, 배포됨) |

## Stage 3. KYC (PASS)
- 거래 주문(`POST /api/orders`)은 `requireKyc('approved')` 미들웨어로 보호. KYC 미승인 계정은 주문 불가. 테스트에서 `kyc_status='approved'` 설정 후에만 주문 성공 확인.

## Stage 4. 입금 / 잔액 (PASS)
- cron(`binaryMatchingTick`)이 `deposits`(status=completed) + `ext_deposits`(credited/swept)를 각 1회만(`binary_counted_at` 마커) 롤업. `admin-*` tx는 제외.
- 입금 USD 환산 정확(USDT/USDC=$1). idempotent 클레임(마커 선점) 확인.

## Stage 5. 거래 (주문/체결/수수료) (PASS)
| 항목 | 결과 |
|---|---|
| 원자적 잔액 차감 | `UPDATE ... WHERE available >= ?` 조건부 차감 (A1 fix) — 동시성 오버커밋 불가 |
| 지정가 매수 체결 | 200, 오더북과 즉시 `filled` |
| 수수료 | quote(USDT) 기준 양방향. 매수자=base 획득+(total+fee) 차감, 매도자=(total−fee) 획득 |
| 서킷브레이커 | 관리자 ON 시 신규 주문 503 차단 (기존 주문 취소는 허용) |
| QTA 일일 매도 캡 | $50,000/1400 상당 초과 시 `QTA_DAILY_SELL_LIMIT` 차단 |

## Stage 6. 출금 (스테이킹 배당 QTA) (PASS)
| 항목 | 결과 |
|---|---|
| 최소 출금 $50 미만 | **400 `BELOW_MIN_WITHDRAWAL`** 차단 |
| 100-QTA 단위 강제 | 위반 시 400 |
| 5% 수수료 | 20,000 요청 → 수수료 1,000 / 순액 19,000 (정확) |
| 잔액 락 | available −20,000 / locked +20,000 (원자적) |
| 주소 검증 | `^0x[0-9a-fA-F]{40}$` 강제 |

## Stage 7. 스테이킹 (PASS)
| 항목 | 결과 |
|---|---|
| $1000 스테이킹 | 280,000 QTA 락 (1000 / 0.00357142857 = 280,000 정확), available/locked 정확 |
| 스폰서-먼저-스테이킹 게이트 | 스폰서 미스테이킹 시 하부 subscribe → **403 `SPONSOR_NOT_STAKED`**, 스폰서 스테이킹 후 → **허용** |
| 포지션 조회 | 원금·경과배당 정확 산출 |
| 조기해지 페널티 | (원금+적립배당) 기준 30% (코드 검증) |

## Stage 8. 바이너리 수동배치 + 매칭 (cron) (PASS — 핵심)
### 8-1. 수동 배치(1회성, 사장님 선택)
| 케이스 | 결과 |
|---|---|
| 신규 하부 | `unplaced`(미배치)로 표시 |
| 좌(L) 배치 | 200 성공 |
| 재배치 시도 | **409 `ALREADY_PLACED`** (1회성·불가역) |
| 남의 하부 배치 | **403 `NOT_YOUR_DOWNLINE`** |
| 미배치(NULL) 하부의 볼륨 | 롤업 **중단**(카운트/파킹 안 함) — 사장님이 좌/우 고를 때까지 대기 |

### 8-2. 매칭 엔진(라이브)
- 3인 트리(UP self $2000, 좌 $1500, 우 $1000): min(1500,1000)=1000 매칭 → 3% → **8,400 QTA**(=$30/0.00357142857) 정확 지급, `matched_usd` 캐리오버로 중복지급 방지. **검증 완료.**

### 8-3. 몸값 0 게이트 + 순서 독립성(결함 #2 재검증)
- 하부가 먼저 입금($3000 좌 / $1000 우), UP 몸값=0 → **전액 pending 파킹, payout 0** (볼륨 손실 없음).
- 이후 UP가 $2000 입금 → 몸값 2000, 캡 4000 → **reclaim 발동**: parked 4000 전액 라이브 복원(pending 0) → min(3000,1000)=1000 매칭 → **8,400 QTA** 지급.
- 즉 **입금 순서(하부 먼저/상부 먼저)와 무관하게 동일 결과** → 결함 #2(순서 의존성) 해결 재확인.

---

## ⚠️ 정책 확인/개선 필요 항목

### [P2-1] "몸값(self_usd)"의 소스가 두 시스템에서 다름 — **정책 확인 요망**
- **바이너리 몸값·좌우 볼륨**: 오직 **입금(deposit/ext_deposit)** 에서만 롤업됨.
- **스폰서-먼저-스테이킹 게이트**: **스테이킹(staking_positions active)** 여부로 판정.
- 결과: 어떤 회원이 **스테이킹만 하고 입금이 없으면** 바이너리 몸값은 0 → 매칭 payout이 안 됨(볼륨은 파킹).
- **질문:** 바이너리 몸값을 **스테이킹 원금도 포함**시켜야 하는지요? (현재는 입금만) 사장님 의도가 "스테이킹=참여자격"이라면 몸값 소스에 스테이킹을 더하는 편이 일관적일 수 있습니다. **코드 변경 전 확답 필요.**

### [P2-2] 200% 진입 정책 (이전 세션부터 대기 중 — 미결)
- 현재: 하부 스테이킹 **진입은 200% 초과와 무관하게 허용**(스폰서 스테이킹 여부만 확인). **매칭 볼륨만** 2×몸값 캡, 초과분은 파킹.
- 선택지: **(A)** 현행 유지(진입 자유·매칭만 캡) vs **(B)** 스폰서 200% 캡 도달 시 하부 진입 자체 차단.
- **사장님 A/B 결정 대기 중 — 확답 없이는 코드 미변경.**

### [P3-1] 낡은 주석 정리 (경미)
- `src/server/routes/auth.ts` L451-454의 "auto-place into smaller leg" 주석은 현행 수동배치와 불일치. 동작 영향 없음. 다음 커밋 시 정리 예정.

---

## 자금 안전 체크리스트 (모두 PASS)
- [x] 주문/스테이킹/출금 모두 **원자적 조건부 차감**(`WHERE available >= ?`) — 동시성 오버커밋 불가
- [x] 이중 클레임 방지(스테이킹 redeem status flip, cron matched_usd 조건부 UPDATE, deposit 마커 선점)
- [x] 회사발행 자금(추천보상/웰컴/배당) `available_initial` 표시 → 외부 출금 불가
- [x] 바이너리 볼륨 손실 없음(over-cap 파킹 + reclaim)
- [x] 웰컴 보너스 중복 지급 불가(firstVerification 가드)

## 결론
가입부터 바이너리 매칭까지 **전 구간 정상**이며 자금 안전 로직이 견고합니다. 출시를 막는 치명 결함은 발견되지 않았습니다. 남은 것은 **P2-1(몸값 소스), P2-2(200% 진입)** 두 정책에 대한 사장님의 결정입니다.
