# QuantaEX 2차 전수조사 리포트 (Pre-Launch QA #2)

**일자**: 2026-08-27
**범위**: 회원가입 → 스테이킹 → 거래 + 레퍼럴 양쪽(가입 QX / 스테이킹 QTA 매칭)
**방식**: 로컬 D1 + 실제 API 라이브 재현 (4인 트리: ROOT → L1 → {L2-left, L2-right})

---

## 종합 판정

| Stage | 항목 | 결과 |
|---|---|---|
| 1 | 회원가입 (추천코드/QX 3단계/바이너리 배치) | ⚠️ 대부분 PASS, **하자 1건** |
| 2 | 스테이킹 (스폰서 게이트/조기해지) | ✅ PASS |
| 3 | 스테이킹 레퍼럴 (바이너리 매칭/self게이트/캡) | ⚠️ PASS, **하자 1건(순서의존성)** |
| 4 | 거래 (KYC/체결/수수료/QTA한도) | ✅ 전부 PASS |
| 5 | 두 레퍼럴 무충돌 | ✅ 라이브 실증 완료 |

**결론: 핵심 로직은 정상. 런칭 전 검토할 하자 2건(바이너리 좌우쏠림 / 매칭 순서의존성) 발견.**

---

## Stage 1 — 회원가입

**검증**: ROOT의 추천코드로 L1 가입, L1의 추천코드로 L2-left/right 가입.

- ✅ **QX 3단계 레퍼럴 정확**: L1가입→ROOT L1레벨 100 / L2가입→L1 100·ROOT L2레벨 50.
  최종 QX 합계: ROOT=200(100+50+50), L1=200(100+100).
- ✅ **회사지급 출금불가 처리**: 지급 QX는 available_initial 동일 증가 → 출금 불가.
- ✅ **3단계 체인/사이클 안전**: referrals 레벨 구분 정확.

### 🔴 하자 #1 — 바이너리 신규배치 좌측 쏠림
- **현상**: L2-left, L2-right가 **둘 다 L1의 L(좌)레그**에 배치됨.
- **원인**: `placeInBinaryTree`가 스폰서의 `left_usd` vs `right_usd`(입금 볼륨) 기준으로 가벼운
  쪽에 배치하는데, 입금 전에는 좌우 볼륨이 모두 0 → 항상 L(동점시 좌). 입금 전에 여러 명이
  붙으면 전원 좌측 쏠림.
- **영향**: 우측 레그가 비어 매칭(min(L,R))이 영원히 0이 될 수 있음. (수동 교정 시 정상 매칭 확인)
- **참고**: 코드 위치 `src/server/lib/binary-matching.ts` L53 `leg = right < left ? 'R' : 'L'`.
- **판단 필요(사장님)**: 신규배치를 (a) 인원수(head count) 기준 좌우 균형, (b) 좌우 번갈아,
  (c) 현행 유지 중 어느 정책으로 할지 결정 필요.

---

## Stage 2 — 스테이킹

- ✅ **스폰서-선스테이킹 게이트**: 스폰서 미스테이킹 시 하부 스테이킹 403 차단.
  ROOT(최상위) 스테이킹 → L1 가능 → L2 가능 순서 정확.
- ✅ **조기해지 30% 페널티**: 원금 140,000 QTA → 반환 98,000(70%)/페널티 42,000(30%) 정확.
- ✅ 잔고 락/해제 정합성 확인.

---

## Stage 3 — 스테이킹 레퍼럴 (바이너리 매칭, cron)

- ✅ **self_usd 게이트**: 본인 입금 0이면 하부 볼륨 자체가 안 쌓이고(2×self 캡=0) 매칭 지급 0.
  → 사장님 규칙("본인 미스테이킹 시 하부 수당 0")과 정확히 일치.
- ✅ **매칭 계산 정확**: L1 self=$2000(캡 $4000), 좌$3000·우$1000(캡초과 유실) → min=$1000 @3%
  = $30 = 8,400 QTA. 지갑·ledger 정확 반영.
- ✅ **2배 캡**: self 대비 초과 하부 볼륨은 정책대로 유실(의도됨).

### 🔴 하자 #2 — 매칭 롤업 순서 의존성
- **현상**: 하부가 상부보다 **먼저** 입금하면, 상부가 나중에 입금(self_usd 상승)해도
  이전 하부 볼륨이 **재롤업되지 않아 영구 유실**됨.
- **원인**: cron이 각 입금을 한 번 처리하면 `binary_counted_at`을 찍어 다시 롤업하지 않음.
  하부 입금 처리 시점의 상부 self_usd(=캡)가 0이면 room=0 → 볼륨 버려짐. 이후 복구 경로 없음.
- **영향**: 상부가 늦게 스테이킹하면 그 이전 하부 실적이 매칭에서 누락 → 수당 과소지급.
- **판단 필요(사장님)**: (a) 상부 self_usd 상승 시 하부 볼륨 재계산(리빌드) 로직 추가, 또는
  (b) 스폰서 선스테이킹 게이트로 사실상 방지되므로 현행 유지(하부는 상부가 스테이킹해야만
  스테이킹/입금 유도) 중 결정 필요.

---

## Stage 4 — 거래 (전부 PASS)

- ✅ **KYC 게이트**: kyc=none 주문 → 403 KYC_REQUIRED.
- ✅ **지정가 체결**: 0.1 BTC @ $60,000 매수/매도 체결.
- ✅ **0.1% 수수료(양쪽 quote/USDT)**: 매도자 +5,994 USDT(6,000−6), 매수자 −6,006 USDT.
- ✅ **QTA 일일 매도 한도**: $71.4 매도 차단(400 QTA_DAILY_SELL_LIMIT), $17.86 매도 통과.
  한도 $35.71(=50,000KRW/1400) 정확.

---

## Stage 5 — 두 레퍼럴 무충돌 (라이브 실증)

한 회원(L1)이 두 레퍼럴을 **동시 수령**한 상태에서:

| 항목 | 가입 레퍼럴 | 스테이킹 레퍼럴 |
|---|---|---|
| 코인 | QX 200 (available_initial 200 = 출금불가) | QTA 8,400 (출금가능) |
| 지갑 레코드 | `wallets(QX)` 별도 행 | `wallets(QTA)` 별도 행 |
| 기록 테이블 | `referrals` (2건) | `binary_match_bonuses` (1건) |

- ✅ 코인·지갑레코드·기록테이블 **완전 분리** → 상호 침범 없음.
- ✅ QX 출금불가 / QTA 출금가능 정책 정확 분리.
- ✅ 거래 잔고(BTC/USDT)까지 독립 유지.

**→ 두 레퍼럴은 라이브에서도 절대 충돌하지 않음 확정.**

---

## 런칭 전 사장님 결정 필요 (신규 2건)

1. **바이너리 신규배치 정책** (하자 #1): 입금 전 좌측 쏠림 → 좌우 균형/번갈아/현행 중 택일.
2. **매칭 순서의존성** (하자 #2): 하부 선입금분 유실 → 재롤업 로직 추가 vs 게이트로 현행 유지.

(기존 리포트 B-1~B-4는 PRE_LAUNCH_QA_REPORT.md 참조. B-1 QTA출금 닫음, B-2 QTA한도 유지, B-3 0008정리 완료.)
