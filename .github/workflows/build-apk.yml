name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Build web app
      run: npm run build
      env:
        VITE_API_URL: https://3001-i9hxkysto1zzwy5b3ntbw-2e77fc33.sandbox.novita.ai
    
    - name: Sync Capacitor
      run: npx cap sync android
    
    - name: Grant execute permission for gradlew
      run: chmod +x android/gradlew
    
    - name: Build Debug APK
      run: |
        cd android
        ./gradlew assembleDebug --no-daemon
    
    - name: Sign APK
      run: |
        echo "APK built successfully!"
        ls -lh android/app/build/outputs/apk/debug/
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: securet-messenger-debug
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
    
    - name: Create Release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: Securet Messenger v1.0.${{ github.run_number }}
        body: |
          ## 📱 시큐렛 메신저 APK
          
          ### 다운로드
          - app-debug.apk (아래 Assets에서 다운로드)
          
          ### 설치 방법
          1. APK 파일 다운로드
          2. Android 기기에서 "알 수 없는 출처" 허용
          3. APK 파일 탭하여 설치
          
          ### 주요 기능
          - 🔐 시큐렛 QR 주소 기반 보안 채팅
          - 📱 QR 코드 스캔으로 친구 추가
          - 💬 실시간 1:1 채팅
          - 👥 이메일 기반 그룹 채팅
          - 📎 파일 공유
          
          ### 버전 정보
          - 빌드 번호: ${{ github.run_number }}
          - 커밋: ${{ github.sha }}
          - 빌드 날짜: ${{ github.event.head_commit.timestamp }}
        files: android/app/build/outputs/apk/debug/app-debug.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
