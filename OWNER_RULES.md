# 지상명령 (OWNER RULES) — 절대 규칙

> ⚠️ 이 문서는 사장님(오너)의 **지상명령**입니다. 여기 적힌 규칙은 임의로 변경 금지.
> 코드가 이 문서와 어긋나면 **문서가 맞고 코드가 틀린 것**으로 간주하고 코드를 고친다.

---

## 0. 🚫 한글(한국어) 화면 표시 절대 금지 — 최우선·무조건

- **사용자 화면(UI)에 표시되는 그 어떤 텍스트도 한국어(한글)로 표시하지 않는다.** 그 어떤 경우에도, 어디에도 예외 없음.
- 토스트/알림/버튼/라벨/에러 메시지 등 **화면에 렌더링되는 모든 문자열은 영어(또는 사용자가 선택한 비-한국어 언어)** 로만 노출.
- 서버 응답의 `message` 필드에 한국어를 넣고 프론트가 그걸 그대로 표시하는 것도 **금지**. 서버는 영어 `message` + `error` 코드만 반환하고, 프론트는 **error 코드 → i18n**으로 현지화한다.
- 코드 주석은 한국어 가능(화면에 안 나오므로). 단, **JSX/문자열 리터럴로 화면에 나가는 한국어는 금지.**
- 한국어 번들(`ko.ts`)은 운영자 전용(숨김 locale)이며 일반 사용자 화면 기본값이 아니다. 기본/폴백 언어는 영어(`en`).

---

## 1. 관리자 인정 스테이킹 (실입금 + 인정금액) — 최우선 규칙

관리자가 회원의 스테이킹을 **실입금 + 인정금액**으로 개설한 경우:

| 항목 | 기준 금액 | 설명 |
|---|---|---|
| **데일리수당** | **총금액 (실입금 + 인정금액)** | 총금액 기준으로 데일리 수당 지급 |
| **매칭보너스** | **총금액 (실입금 + 인정금액)** | 몸값(self_usd)·볼륨·매칭 전부 총금액 기준 |
| **원금 반환 (만기/해지)** | **실입금만** | 인정금액은 반환하지 않고 **소멸** |

### 예시
- 관리자가 실입금 $1,000 + 인정금액 $10,000 = **총 $11,000**으로 개설
- → 데일리수당: **$11,000** 기준으로 계산
- → 매칭보너스: 몸값 **$11,000**, 하부 볼륨·매칭 전부 $11,000 기준
- → 만기 원금 반환: **$1,000(실입금)만** 반환, $10,000(인정금액)은 소멸

### 코드 위치 (2026-08-31 기준, 규칙과 일치 확인됨)
- 총금액 = `principal_usd` = `realUsd + bonusUsd` — `src/server/routes/admin.ts` (grant 핸들러)
- 데일리수당: `accruedUsd()`가 `principal_usd`(총금액) 사용 — `src/server/routes/earn.ts`
- 매칭 롤업: `rollStakeUpBinary(db, user_id, principalUsd, ...)` 총금액 기준 — `src/server/routes/admin.ts`
- 원금 반환: `returnedQta = realPrincipalQta`(실입금만) — `src/server/routes/earn.ts` (redeem)
- 실입금 보관 컬럼: `staking_positions.real_principal_usd`, 인정금액: `bonus_principal_usd`

---

## 2. 데일리수당 규칙

- **기준**: 진입금액(총금액) × 데일리 요율(daily_rate) × 경과일수
- **시작 시점**: **익일부터** (진입 당일은 0일치, 지급 없음)
- **지급 단위**: 하루 **1일치씩**
- **날짜 기준**: **한국 표준시(KST, UTC+9) 자정**에 날짜가 바뀔 때마다 +1일치
  - 토요일 진입 → 당일 0 → 일요일 1일치 → 월요일 2일치(일+월)
  - 진입한 시각과 무관하게 **한국 날짜가 바뀌면** 하루치 발생
- **상한**: term_days(만기)까지만 적립
- **코드**: `accruedUsd()` + `kstDayIndex()` — `src/server/routes/earn.ts`
- **중복 지급 방지**: claim 시 `accrued_dividend_usd` 스냅샷 원자 가드 (claim-first)

---

## 3. 매칭보너스 규칙 (바이너리 좌/우)

- **기준**: 소실적(약한 레그 = min(좌 볼륨, 우 볼륨))
- **지급 방식**: 소실적이 각 **단계(티어) 목표에 도달할 때, 그 단계 1회만** 지급
- **단계별 목표·요율 (5단계)**:

| 소실적 도달 | 요율 | 단계 지급액 |
|---|---|---|
| $1,000 | 3% | $30 |
| $5,000 | 4% | $200 |
| $10,000 | 5% | $500 |
| $50,000 | 6% | $3,000 |
| $100,000 | 8% | $8,000 |

- **1회성**: 이미 지급한 단계는 재지급 없음 (`matched_usd` = 지급한 최고 목표, 원자 가드)
- **자격**: 본인이 스테이킹(몸값 self_usd > 0)해야 매칭 수령 가능
- **레그당 2× 몸값 하드캡**: 각 레그 독립적으로 `2 × self_usd`까지만 인정, 초과분 드롭
- **코드**: `MATCH_TIERS`, `runMatchForUser()`, `rollStakeUpBinary()` — `src/server/lib/binary-matching.ts` (웹서버 동기) + `cron-worker/src/binary-matching.ts` (안전망)

---

## 변경 이력
- 2026-08-31: 최초 작성. 관리자 인정 스테이킹(총금액 데일리·매칭 / 실입금 반환), 데일리 KST 자정 기준, 매칭 5단계 1회성 규칙 못박음.
