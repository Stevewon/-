# 지상명령 (OWNER RULES) — 절대 규칙

> ⚠️ 이 문서는 사장님(오너)의 **지상명령**입니다. 여기 적힌 규칙은 임의로 변경 금지.
> 코드가 이 문서와 어긋나면 **문서가 맞고 코드가 틀린 것**으로 간주하고 코드를 고친다.

---

## 0. 🚫 한글(한국어) 화면 표시 절대 금지 — 최우선·무조건

- **사용자 화면(UI)에 표시되는 그 어떤 텍스트도 한국어(한글)로 표시하지 않는다.** 그 어떤 경우에도, 어디에도 예외 없음.
- 토스트/알림/버튼/라벨/에러 메시지 등 **화면에 렌더링되는 모든 문자열은 영어(또는 사용자가 선택한 비-한국어 언어)** 로만 노출.
- 서버 응답의 `message` 필드에 한국어를 넣고 프론트가 그걸 그대로 표시하는 것도 **금지**. 서버는 영어 `message` + `error` 코드만 반환하고, 프론트는 **error 코드 → i18n**으로 현지화한다.
- 코드 주석은 한국어 가능(화면에 안 나오므로). 단, **JSX/문자열 리터럴로 화면에 나가는 한국어는 금지.**
- 한국어 번들(`ko.ts`)은 운영자 전용(숨김 locale)이며 일반 사용자 화면 기본값이 아니다. 기본/폴백 언어는 영어(`en`).

---

## 1. 관리자 인정 스테이킹 (실입금 + 인정금액) — 최우선 규칙

관리자가 회원의 스테이킹을 **실입금 + 인정금액**으로 개설한 경우:

| 항목 | 기준 금액 | 설명 |
|---|---|---|
| **데일리수당** | **총금액 (실입금 + 인정금액)** | 총금액 기준으로 데일리 수당 지급 |
| **매칭보너스** | **총금액 (실입금 + 인정금액)** | 몸값(self_usd)·볼륨·매칭 전부 총금액 기준 |
| **원금 반환 (만기/해지)** | **실입금만** | 인정금액은 반환하지 않고 **소멸** |

### 예시
- 관리자가 실입금 $1,000 + 인정금액 $10,000 = **총 $11,000**으로 개설
- → 데일리수당: **$11,000** 기준으로 계산
- → 매칭보너스: 몸값 **$11,000**, 하부 볼륨·매칭 전부 $11,000 기준
- → 만기 원금 반환: **$1,000(실입금)만** 반환, $10,000(인정금액)은 소멸

### 코드 위치 (2026-08-31 기준, 규칙과 일치 확인됨)
- 총금액 = `principal_usd` = `realUsd + bonusUsd` — `src/server/routes/admin.ts` (grant 핸들러)
- 데일리수당: `accruedUsd()`가 `principal_usd`(총금액) 사용 — `src/server/routes/earn.ts`
- 매칭 롤업: `rollStakeUpBinary(db, user_id, principalUsd, ...)` 총금액 기준 — `src/server/routes/admin.ts`
- 원금 반환: `returnedQta = realPrincipalQta`(실입금만) — `src/server/routes/earn.ts` (redeem)
- 실입금 보관 컬럼: `staking_positions.real_principal_usd`, 인정금액: `bonus_principal_usd`

---

## 2. 데일리수당 규칙

- **기준**: 진입금액(총금액) × 데일리 요율(daily_rate) × 경과일수
- **시작 시점**: **익일부터** (진입 당일은 0일치, 지급 없음)
- **지급 단위**: 하루 **1일치씩**
- **날짜 기준**: **한국 표준시(KST, UTC+9) 자정**에 날짜가 바뀔 때마다 +1일치
  - 토요일 진입 → 당일 0 → 일요일 1일치 → 월요일 2일치(일+월)
  - 진입한 시각과 무관하게 **한국 날짜가 바뀌면** 하루치 발생
- **상한**: term_days(만기)까지만 적립
- **★ 포지션별 개별 계산 (2026-09-04 지상명령)**: 한 회원이 서로 다른 날 여러 번 진입하면(예: 오늘 $1,000, 한 달 뒤 $5,000), **각 포지션은 자기 금액·자기 요율(daily_rate)·자기 기간(term_days)·자기 시작일(created_at)로 완전히 개별 계산**한다. 절대 합산해서 뭉뚱그리지 않는다. (코드: `accruedQta()`/`accruedDays()`가 포지션 `p`별로 계산, positions API가 포지션마다 개별 map 처리.)
- **코드**: `accruedUsd()` + `kstDayIndex()` — `src/server/routes/earn.ts`
- **중복 지급 방지**: claim 시 `accrued_dividend_usd` 스냅샷 원자 가드 (claim-first)

---

## 3. 매칭보너스 규칙 (바이너리 좌/우)

- **기준**: 소실적(약한 레그 = min(좌 볼륨, 우 볼륨))
- **지급 방식**: 소실적이 각 **단계(티어) 목표에 도달할 때, 그 단계 1회만** 지급
- **단계별 목표·요율 (5단계)**:

| 소실적 도달 | 요율 | 단계 지급액 |
|---|---|---|
| $1,000 | 3% | $30 |
| $5,000 | 4% | $200 |
| $10,000 | 5% | $500 |
| $50,000 | 6% | $3,000 |
| $100,000 | 8% | $8,000 |

- **1회성**: 이미 지급한 단계는 재지급 없음 (`matched_usd` = 지급한 최고 목표, 원자 가드)
- **자격**: 본인이 스테이킹(몸값 self_usd > 0)해야 매칭 수령 가능
- **★ 볼륨은 무한대 (2026-09-04 정정)**: 좌우 볼륨(`left_usd`/`right_usd`)은 **상한 없이 하부 실적 전부 반영**. 이전의 "레그당 2× 몸값 볼륨 하드캡·드롭"은 **폐기**. 예) namim 몸값 $1,000, 왼쪽에 3명 총 $6,000 → `left_usd = $6,000`(전부 반영).
- **★ 매칭수당 총 한도 = 몸값 × 2 (2026-09-04 정정)**: 한 회원이 **평생 받을 수 있는 매칭수당 누계**는 **본인 몸값(self_usd)의 2배(USD)** 를 절대 초과 못 함(좌우 통합 상한). 소실적(min(좌,우)) 기준으로 티어 매칭을 계산하되, 누계가 `2 × self_usd`에 도달하면 그 이후는 지급 0. 예) 몸값 $1,000 → 매칭 평생 한도 $2,000. 좌 $500k/우 $1M라 소실적 $500k 티어가 $20,000을 주더라도 **$2,000까지만** 수령. 추후 본인이 몸값을 늘리면 한도가 커져 재개될 수 있음.
- **★ 몸값(self_usd)은 진입 시마다 누적 합산 (2026-09-04 지상명령)**: 본인 계정에서 **서로 다른 날 여러 번 진입**하면 각 진입 금액은 몸값에 **누적 합산**된다. 데일리 배당은 포지션별로 개별 계산(위 2번 규칙)이지만, **몸값(self_usd)은 모든 포지션 진입금액의 합**이다.
  - 예) 오늘 $1,000 진입(요율 A, 기간 A) → 몸값 $1,000. 한 달 뒤 별도로 $5,000 진입(요율 B, 기간 B) → **$5,000 진입한 그날부터 몸값 = $6,000**($1,000 + $5,000).
  - 이때 두 포지션의 **데일리는 각자의 요율·기간·시작일로 절대 개별 계산**(합산 금지). 하지만 **몸값 총액은 $6,000**으로 인정된다.
  - **하부 진입 상한 = 몸값 × 2**: 몸값 $6,000이 되는 순간부터 **직속 좌/우 하부는 각각 최대 $12,000까지 진입** 가능(매칭수당 총 한도 기준도 $12,000으로 상승).
  - (코드: `rollStakeUpBinary()`가 `!skipSelf`일 때 `self_usd = self_usd + ?`로 누적 — `src/server/lib/binary-matching.ts:317`. 매칭 한도·하부 기준 = 누적된 `selfUsd × 2`.)
- **★ 데일리와 별개 (2026-09-04)**: 위 매칭 200% 한도는 **데일리 배당과 완전 별개**. 데일리는 한도 없이 매일(스테이킹 기간 내) 지급.
- **한도 추적 기준**: 매칭은 QTA로 지급되나 시세 변동 때문에 한도는 **지급 시점 USD 가치(`bonus_usd`) 누계**로 관리 (`SUM(bonus_usd) < self_usd × 2`).
- **코드**: `MATCH_TIERS`, `runMatchForUser()`(매칭 2× 몸값 USD 누계 clamp), `rollStakeUpBinary()`(볼륨 무한 롤업) — `src/server/lib/binary-matching.ts` (웹서버 동기) + `cron-worker/src/binary-matching.ts` (안전망)

---

## 4. QTA 거래/시장운영 정책 (2026-09-05 지상명령)

QTA/USDT 마켓에만 적용. 회사(마켓메이커) = **mm-bot-a / mm-bot-b** (봇 계정, 데이터 수정 시 제외 대상).

- **★ 테더(USDT) 시세 = 1,450원 고정** (별도 지시 전까지). 매수·매도 환산 모두 1 USDT = 1,450 KRW. 코드 상수 `USDT_KRW_RATE = 1450`.
- **★ 24h 평균 하한선 (핵심 어뷰징 방어)**: **전일(어제 KST 0~24시) 거래량가중 평균 체결가** 이하 가격에서는 **어떤 매도도 체결 불가**(회원끼리든 회사든 전부). 전일 체결 이력이 없으면 **관리가격(`price_center`)** 을 하한선으로 사용.
  - 이유: 누군가 1원에 매도 → 회사가 (5만원어치) 사줌 → 그 사이 1원에 1억원어치 매수로 후려쳐 사가려는 자전거래 차단. 1원은 24h 평균 한참 아래이므로 **어떤 체결도 안 됨**.
  - 코드: `qtaPriceFloor()` + `matchOrder()` 체결 루프에서 `tradePrice < floor`이면 스킵 — `src/server/routes/order.ts`.
- **★ 매수 (회원이 QTA 삼) = 무제한**: 1 USDT=1,450원 환산, 나눠 사도 됨.
  - 체결 순서: **① 순수 매도인(일반 회원) 물량 먼저 → ② 부족분은 회사(mm-bot-a)가 매수인 주문가격 그대로 공급**해 100% 체결.
  - 단 **매수 주문가격이 현재 시세(mid) 미만이거나 24h 하한선 미만이면 회사는 공급 안 함** → 그 매수는 미체결로 방치("호가 시세 이하면 사주지 말라").
- **★ 매도 (회원이 QTA 팖)**: 시세 이하로 내놔도 **회사(mm-bot-b)가 무조건 실시간 매수**해 즉시 팔리게 해줌 — **단 회원 1인당 하루 5만원어치(= 50000/1450 ≈ 34.48 USDT)까지만**.
  - **★ 초과분 자동취소 (2026-09-05 정책수정)**: 하루 한도 5만원어치가 팔리면, 그 회원의 **남은 매도 주문은 시스템이 자동으로 전량 취소** — **매도호가창에서 제거 + 잠긴 QTA를 `available`로 환불**. (이전 '방치'에서 변경. 회원이 직접 취소할 필요 없음.)
  - 회사가 사줄 때 **매도인이 부른 가격(ask price)** 으로 체결(매도인은 자기 가격에 팔림), 단 24h 하한선 이상일 때만.
  - 개인당 한도 추적: 오늘 회사(mm-bot)가 그 회원에게서 사준 USDT 누계 기준. 코드 상수 `MM_MEMBER_BUY_BUDGET_USDT = 50000/1450`.
- **★ 체결 타이밍 = 즉시 아님, 1분 이내**: 회원 매수/매도 주문은 즉시 접수·호가 등재만 하고, 회사 개입 체결은 **1분 주기 cron(`* * * * *`)** 이 처리 → 주문 후 **1~59초 내 체결**. (Cloudflare 환경 안정성 위해 대기열 방식. 로직은 서버 `qta-mm-tick`에 있고 cron은 트리거만.)
- **코드**: `qtaPriceFloor()`/`matchOrder()`/`qta-mm-tick`(Step1 매도 실시간매수·개인5만원, Step1.5 매수 즉시공급) — `src/server/routes/order.ts`; cron 1분 트리거 — `cron-worker/wrangler.jsonc` + `cron-worker/src/index.ts`(`qtaMmTick`).

---

## 변경 이력
- 2026-08-31: 최초 작성. 관리자 인정 스테이킹(총금액 데일리·매칭 / 실입금 반환), 데일리 KST 자정 기준, 매칭 5단계 1회성 규칙 못박음.
- 2026-09-04: **바이너리 규칙 근본 정정.** ① 좌우 볼륨은 무한대(하부 실적 전부 반영, 이전 볼륨 2× 하드캡·드롭 폐기). ② 매칭수당 총 한도 = 본인 몸값 × 2(USD 누계, 좌우 통합). ③ 데일리와 매칭 200% 한도는 별개. 전체 라인 볼륨 재산정(namim 좌 $2,000→$6,000 등), 매칭 지급 총액 불변(268,250 QTA).
- 2026-09-04: **지상명령 문서화(코드 이미 준수, 문서만 보강).** ① 데일리 배당은 **포지션별 개별 계산**(자기 요율·기간·시작일, 합산 금지) — 2번 규칙에 명시. ② 몸값(self_usd)은 **진입 시마다 누적 합산**(예: $1,000 + 한 달 뒤 $5,000 = 그날부터 $6,000) → 하부 좌/우 각각 **$12,000까지 진입** 가능(매칭 한도 기준도 $12,000) — 3번 규칙에 명시. 코드 검증: `accruedQta`/`accruedDays`(포지션별), `self_usd = self_usd + ?`(binary-matching.ts:317, 누적), `matchCapUsd = selfUsd × 2` — 전부 준수 확인.
- 2026-09-05: **QTA 거래/시장운영 정책 신설(4번 섹션).** 테더 1,450원 고정 / 24h 평균 하한선 이하 매도 체결 금지(어뷰징 방어) / 매수 무제한(순수매도인 먼저→회사 매수인가격 공급, 시세·하한선 미만은 공급안함) / 매도는 회사가 회원당 하루 5만원어치까지 실시간 매수 / 체결은 1분 주기 cron으로 1~59초 내. matchOrder 하한선 게이트 + qta-mm-tick 재작성 + cron 1분 트리거 추가. 매도 일일한도 환율 1400→1450, POST 단계 거부 폐지.
- 2026-09-05: **매도 초과분 정책 수정 — 방치 → 자동취소·환불.** 하루 회사매수 한도(5만원) 도달 회원의 잔여 QTA 매도주문을 mm-tick에서 전량 자동 취소하고 잠긴 QTA를 available로 환불(호가창에서 제거). `qta-mm-tick` Step1에 auto-cancel sweep 추가.
